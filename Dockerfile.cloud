# 云部署优化的Dockerfile
FROM node:18-alpine

# 设置工作目录
WORKDIR /app

# 安装必要的包
RUN apk add --no-cache g++ python3 make

# 复制package文件
COPY package*.json ./

# 安装依赖
RUN npm ci --only=production

# 复制应用代码
COPY . .

# 创建临时目录
RUN mkdir -p temp && chmod 777 temp

# 暴露端口
EXPOSE 3002

# 启动应用
CMD ["npm", "start"]